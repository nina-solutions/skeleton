mixin facade(name)
  if name
    |{{{facade "#{name}"}}}
  else
    |{{{facade}}}

mixin css(link)
  link(type='text/css', rel='stylesheet', href!='{{{static "#{link}"}}}')

mixin static(link)
  |{{{static "#{link}"}}}