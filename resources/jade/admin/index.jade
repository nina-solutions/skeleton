include ../partials/mixins
+extends('admin/partials/template')

+section('title')
  title @lang('admin.'.$table->name.'.head')

+section('breadcrumbs')
  ol.breadcrumb
    li
      a(href='/admin/dashboard')
        i.fa.fa-dashboard
        | @lang('admin.dashboard.head')
    li.active
      | @lang('admin.'.$table->name.'.head')

+section('inner-title')
  h1 @lang('admin.'.$table->name.'.head')
    small @lang('admin.'.$table->name.'.head')

+section('content')
  .row
    .col-xs-12
      .box
        .box-header
          a.btn.btn-primary(href!="{{ action($table->controller.'@create') }}")
            h3.box-title @lang('admin.actions.create')
          +include('admin.partials.index.search')
        +include('admin.partials.index.table')
      + php("$data->render()")